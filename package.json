{
  "name": "thank-me-later",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "oxlint --type-aware",
    "fmt": "oxfmt --write .",
    "fmt:check": "oxfmt --check .",
    "build": "bun build packages/cli/src/index.ts --compile --outfile dist/tml",
    "build:binaries": "bash scripts/build-binaries.sh",
    "test": "bun test",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "release": "bash scripts/release.sh"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "@types/bun": "latest",
    "oxfmt": "^0.54.0",
    "oxlint": "^1.69.0",
    "oxlint-tsgolint": "^0.23.0",
    "tml": "workspace:*",
    "typescript": "^5.6.0"
  }
}
