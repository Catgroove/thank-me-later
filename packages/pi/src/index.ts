// @tml/pi — the pi host Adapter: registers a trigger and consumes tml's event
// stream to render progress inside the pi harness (ADR-0002). The real adapter
// surface lands in a later spec. Placeholder export keeps the package valid.
export const PACKAGE = "@tml/pi" as const;
