---
"tml": patch
---

Add `--help`/`-h` (and bare `tml`) to print usage listing the `ship` and `init` commands and their flags. Command-level `tml ship --help` / `tml init --help` show the same help.
