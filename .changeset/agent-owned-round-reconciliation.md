---
"@tml/core": patch
"@tml/defaults": patch
"tml": patch
---

Replace cross-round finding-id stall detection with agent-owned verification, a no-progress stop when fixes produce no commit, and a configurable `maxFixAttempts` `tml.json` knob.
