---
"@tml/view": patch
---

Group the approval drawer's findings by action - "Needs your decision" (ask-user), "Auto-fix next round", and "Informational" - each with its own icon, accent color, and header count, mirroring the inspector's findings tab. Finding rows now color by severity and drop the redundant per-row action tag, and keyboard navigation walks the same section order so the focus highlight always lines up.
