---
"@tml/view": patch
---

Show a static amber glyph instead of the busy spinner on an active Step that is blocked awaiting a human decision (an `ask`/`approval` gate), so the pipeline rail reads as "waiting on you" rather than "working".
